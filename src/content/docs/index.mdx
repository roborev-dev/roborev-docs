---
title: roborev
description: Continuous code review for coding agents
hero:
  tagline: Continuous code review for coding agents. Review commits immediately, catch issues early, and fix them while context is fresh.
  image:
    html: <video src="/tui-hero.webm" autoplay muted loop playsinline />
  actions:
    - text: Read Docs
      link: /quickstart/
      icon: right-arrow
    - text: View on GitHub
      link: https://github.com/roborev-dev/roborev
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<div class="agent-matrix">
  <a href="/agents/"><img src="/agents/codex.svg" alt="Codex" data-agent="codex" width="1604" height="719" /></a>
  <a href="/agents/"><img src="/agents/claude-code.svg" alt="Claude Code" data-agent="claude-code" width="696" height="95" /></a>
  <a href="/agents/"><img src="/agents/gemini.svg" alt="Gemini" data-agent="gemini" width="344" height="127" /></a>
  <a href="/agents/"><img src="/agents/opencode.svg" alt="OpenCode" data-agent="opencode" width="641" height="115" /></a>
  <a href="/agents/"><img src="/agents/copilot.svg" alt="Copilot" data-agent="copilot" width="419" height="95" /></a>
  <a href="/agents/"><img src="/agents/cursor.svg" alt="Cursor" data-agent="cursor" width="800" height="190" /></a>
  <a href="/agents/"><span class="agent-text">Droid</span></a>
</div>

## Quick Start

```bash
curl -fsSL https://roborev.io/install.sh | bash
```

Then from within your git repositories:

```bash
roborev init          # Install post-commit hook
# do some work, generate commits
roborev fix           # Fixes unaddressed reviews
roborev tui           # View reviews in interactive UI
```

For Windows, see the [installation guide](/installation/#quick-install-recommended).

## Why roborev?

AI coding agents write code fast, but they make mistakes. Most review feedback comes too late. The agent has moved on and context is lost. roborev changes this:

1. **Ask your agents to commit often**, ideally every turn of work
2. **roborev reviews** each commit in the background
3. **Feed findings** into your agent sessions, or fix them autonomously with `roborev fix`

Every commit gets reviewed. Issues surface in seconds, not hours.

<CardGrid stagger>
  <Card title="Background Reviews" icon="pencil">
    Every commit is reviewed automatically via git hooks. No workflow changes required.
  </Card>
  <Card title="Auto-Fix" icon="setting">
    `roborev fix` feeds review findings to an agent that applies fixes and commits.
    `roborev refine` iterates until reviews pass.
  </Card>
  <Card title="Code Analysis" icon="document">
    Built-in analysis types (duplication, complexity, refactoring, test fixtures, dead code) that agents can fix automatically.
  </Card>
  <Card title="Multi-Agent" icon="rocket">
    Works with Codex, Claude Code, Gemini, Copilot, OpenCode, Cursor, and Droid. Auto-detects installed agents.
  </Card>
  <Card title="Runs Locally" icon="laptop">
    No hosted service or additional infrastructure. Reviews are orchestrated on your machine using the coding agents you already have configured.
  </Card>
  <Card title="Multi-Machine Sync" icon="external">
    Bi-directionally sync reviews across machines via PostgreSQL.
  </Card>
</CardGrid>

## Architecture

<img src="/architecture.svg" alt="roborev architecture diagram" class="diagram-center" />

- **Daemon**: HTTP server on port 7373 (auto-finds available port if busy)
- **Workers**: Pool of 4 (configurable) parallel review workers
- **Storage**: SQLite at `~/.roborev/reviews.db` with WAL mode
- **Config**: Global at `~/.roborev/config.toml`, per-repo at `.roborev.toml`

## Federated Multiplayer

<img src="/federation.svg" alt="roborev federation diagram" class="diagram-center" />

Bi-directionally sync reviews across machines with a shared PostgreSQL database. Each daemon maintains its local SQLite for fast access while syncing changes to the central database.
